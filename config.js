/* ============================================================
   SquirrelSplit configuration
   ------------------------------------------------------------
   apiUrl: paste the Google Apps Script Web App URL here after
           you deploy it (it ends in .../exec). See README.md.

   Leave it as "DEMO" to try the app with sample data on your
   own device (nothing is saved to the cloud in DEMO mode —
   it just uses your browser's local storage so you can play).
   DEMO password is:  squirrel
   ============================================================ */
window.SQUIRREL_CONFIG = {
  apiUrl: "DEMO",
};
